// Markdown rendering configuration
// Used by react-markdown in ModelResponse and MasterVerdict components

export const markdownComponents = {
  // Custom code block renderer can be added here if needed
};
